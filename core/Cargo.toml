[package]
name = "speare"
version = "0.0.1"
edition = "2021"

[dependencies]
async-trait = { version = "0.1.73" }
flume = "0.11.0"
tokio = { version = "1.32.0", features = ["macros", "rt", "sync", "time"] }
